export default function DashboardPage() {
  // top-level overview — pulls summary numbers from every other feature's API
  return (
    <div>
      <h2 className="text-lg font-medium">DashboardPage</h2>
    </div>
  )
}
