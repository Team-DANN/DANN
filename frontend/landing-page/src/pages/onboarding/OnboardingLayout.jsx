// Shared wrapper for all onboarding steps — progress bar, back button, step transitions.
// TODO: build once individual steps are defined.

export default function OnboardingLayout({ children }) {
  return <div>{children}</div>;
}